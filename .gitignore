# macOS
.DS_Store

# 의존성
node_modules/

# Next.js 빌드 산출물
.next/
out/
build/

# 환경변수 (API 키 — 절대 커밋 금지)
.env
.env.local
.env*.local

# 배포
.vercel/

# 로그
*.log
npm-debug.log*

# 노션용 기획 문서 — Public 저장소라 커밋하지 않는다 (로컬·노션에만 보관)
노션_*.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
.vercel
.env*

# 루트의 기획·발표 산출물 — Public 저장소라 올리지 않는다. 덱 원본은 deck/에 있다.
# (루트 .md는 인수인계·설계도처럼 일부러 올리는 것이 있어 막지 않았다 — git add 전에 확인)
/*.pdf
/*.docx
/기업연계_세호/
/기업연계_현지/

# 개인 도구 설정
/.claude/
/.mcp.json
/.ignore

# 개인 작업 — 팀 저장소에 올리지 않는다
/포트폴리오_수정안.html
