# macOS
.DS_Store

# 의존성
node_modules/

# Next.js 빌드 산출물
.next/
out/
build/

# 환경변수 (API 키 — 절대 커밋 금지)
.env
.env.local
.env*.local

# 배포
.vercel/

# 로그
*.log
npm-debug.log*

# 노션용 기획 문서 — Public 저장소라 커밋하지 않는다 (로컬·노션에만 보관)
노션_*.md

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
.vercel
.env*
