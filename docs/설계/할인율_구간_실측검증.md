# 할인율 구간 — 실측 검증과 수정

2026-09-18 · 대상 문서: `할인율 세분화 기획안 v4` §4 · §17 · §19~20 · §24 · §26-5

기획안의 네 축(시장 30% + 하락장 3%p + 수요 3%p + 재고 2%p = 38%) **구조는 그대로 둔다.**
바뀌는 것은 **1단계 KOSPI 변동폭 구간 경계 하나**다.

---

## 1. 왜 고치는가

초안의 경계(1.5 / 3.0 / 4.5%)를 KOSPI 실측 분포로 재보면 대부분의 날이 최하단에 몰린다.

**Yahoo `^KS11` 일봉 5년 · 1,218 거래일 · 평균 |일간 변동| 1.16%** (직접 집계)

| 초안 구간 | 기본 할인 | 실측 일수 | 비율 | 연 환산 |
|---|---:|---:|---:|---:|
| 0 ~ 1.5% | 5% | 934일 | **76.7%** | 188일 |
| 1.5 ~ 3.0% | 10% | 203일 | 16.7% | 41일 |
| 3.0 ~ 4.5% | 20% | 37일 | 3.0% | 7일 |
| 4.5% 이상 | 30% | 44일 | 3.6% | 9일 |

→ **4일 중 3일이 5%.** "매일 폭이 다르다"가 성립하지 않는다.
→ **30%는 연 9일.** 발표 시연에서 보여줄 수도 없고, 38%는 사실상 도달 불가다.

기본 할인 기대값은 7.2%로, 할인 비용은 낮지만 서비스의 핵심 장치(매일 바뀌는 폭)가 죽는다.

---

## 2. 확정 — 5구간

`§4. 1단계 — KOSPI 변동폭으로 시장 기본 할인율 결정` 표를 이것으로 교체한다.

| KOSPI 절대 변동폭 | 시장 기본 할인율 | 실측 일수 | 비율 | 연 환산 |
|---|---:|---:|---:|---:|
| 0.3% 미만 | 5% | 270일 | 22.2% | 54일 |
| 0.3 ~ 0.8% | 10% | 342일 | 28.1% | 69일 |
| 0.8 ~ 1.5% | 15% | 322일 | 26.4% | 65일 |
| 1.5 ~ 2.5% | 20% | 172일 | 14.1% | 35일 |
| 2.5% 이상 | 30% | 112일 | **9.2%** | 23일 |

**기본 할인 기대값 13.5%**

이 구성의 값어치는 둘이다.

1. **아래 세 구간이 거의 균등**(22 / 28 / 26%)해 열흘 중 여드레는 폭이 서로 다르다
2. **최상단 30%는 연 23일**로 희귀하지만 실제로 온다 — *"1년에 23일, 이런 날이 옵니다"* 로 말할 수 있다

---

## 3. 후보 비교 (같은 1,218 거래일)

| 안 | 경계 | 할인율 | 기본 기대값 | 30% 빈도 | 판단 |
|---|---|---|---:|---:|---|
| 초안 | 1.5 / 3.0 / 4.5 | 5·10·20·30 | 7.2% | 연 9일 | ✗ 76.7%가 5%에 몰림 |
| A | 0.3 / 0.8 / 1.5 | 5·10·15·20 | 12.5% | — | 균등하나 상단이 20%로 낮아 38% 구조와 안 맞음 |
| C | 0.3 / 0.8 / 1.5 | 5·10·20·30 | **16.2%** | 연 57일 | ✗ 30%가 연 57일 — 기업 부담 과다 |
| **D (확정)** | 0.3 / 0.8 / 1.5 / 2.5 | 5·10·15·20·30 | **13.5%** | 연 23일 | ✅ 균등 + 상단 희귀 + 도달 가능 |
| E | 0.3 / 0.8 / 1.5 / 2.5 | 5·8·12·18·30 | 11.8% | 연 23일 | 비용은 더 낮지만 중간 구간 체감차가 줄어듦 |

> D를 고른 기준: **"열흘 중 여드레는 폭이 다르고, 최대 폭은 1년에 20여 일"**.
> 기대값 13.5%는 현행 코드(12.5%)와 1%p 차이로, 기업 입장에서 급격한 증가가 아니다.

---

## 4. 38% 구조는 그대로

기본 상단이 30%이므로 기획안의 배분이 변하지 않는다.

```text
KOSPI 변동폭     최대 30%
하락장 보정          +3%p
SKU 수요 보정        +3%p
SKU 재고 보정        +2%p
                 ───────
                    38%    ← 규칙 자체의 최대합. 절삭 불필요
```

따라서 §9-1 · §17 · §20 · §26-5의 계산과 예시는 **기본값만 새 표로 바꿔 읽으면 된다.**

### §18 예시 1 재계산 — KOSPI −2.2%

변동폭 2.2% → **1.5~2.5% 구간 → 기본 20%** (초안에서는 10%였다)

| 상품 | 수요 | 재고 | 하락장 | 최종 |
|---|---:|---:|---:|---:|
| 카스테라 | 0 | 0 | +3 | **23%** |
| 휘낭시에 | +2 | +1 | +3 | **26%** |
| 티라미수 | +3 | +2 | +3 | **28%** |

### §20 예시 3 재계산 — KOSPI −5.0%

변동폭 5.0% → **2.5% 이상 → 기본 30%**

```text
30 + 3(수요) + 2(재고) + 3(하락장) = 38%
```

초안과 같은 결론(38%)에 도달하지만, **경계가 2.5%라 연 23일 실제로 열린다.**

---

## 5. 구현 상태

### 코드 — 반영 완료

`web/src/data/indicators.ts`의 `DISCOUNT_TIERS`를 5구간으로 교체했다.
실측 분포와 "초안 경계를 쓰지 않는 이유"를 주석으로 남겼다.

```ts
{ minAbsChange: 2.5, rate: 0.30 }   // 아주 큰 움직임
{ minAbsChange: 1.5, rate: 0.20 }   // 매우 큰 움직임
{ minAbsChange: 0.8, rate: 0.15 }   // 큰 움직임
{ minAbsChange: 0.3, rate: 0.10 }   // 중간 움직임
{ minAbsChange: 0,   rate: 0.05 }   // 작은 움직임
```

상한 `MAX_DISCOUNT_RATE = 0.38`(기업 확인값)은 그대로다.

### ⚠️ 저장소 — 운영자가 한 번 해야 한다

`loadTiers()`는 **DB(`discount_tiers`)에 행이 있으면 코드 상수를 무시한다.**
배포본에는 이전 4구간이 들어 있어, 아래 중 하나를 하지 않으면 화면이 안 바�뀐다.

**방법 1 — 관리자 화면** `/admin` → 구간 설정에서 5줄로 저장

**방법 2 — Supabase SQL 에디터**

```sql
delete from discount_tiers;
insert into discount_tiers (min_abs_change, rate, label) values
  (2.50, 0.300, '아주 큰 움직임'),
  (1.50, 0.200, '매우 큰 움직임'),
  (0.80, 0.150, '큰 움직임'),
  (0.30, 0.100, '중간 움직임'),
  (0.00, 0.050, '작은 움직임');
```

### 아직 코드에 없는 것

| 축 | 상태 | 막는 것 |
|---|---|---|
| 시장 기본 할인 (30%) | **동작** | — |
| 하락장 +3%p | **동작** | — (2026-09-18 구현) |
| SKU 수요 +3%p | 미구현 | 관심·장바구니·구매의 **SKU별 서버 집계** 필요 (관심은 현재 브라우저에만 저장) |
| SKU 재고 +2%p | 미구현 | **재고 수량·판매 이력** 필요 — 기업 제공 |
| SKU별 마진 상한 | 미구현 | 상품별 원가·허용 할인율 — 기업 확인 |

> 기업 데이터 없이 구현 가능한 **하락장 +3%p는 반영 완료**다.
> 수요·재고 보정은 발표에서 **설계 문서 + 샘플 시뮬레이션**으로 제시하는 것이 정직하다.

#### 하락장 +3%p — 반영 내용

방향은 이미 `moodFor()`가 판정하고 있었다. 기분(위로가/자축가)만 바꾸고 가격은
같았던 것을 실제 가격으로 연결했다.

```ts
// web/src/data/indicators.ts
export const DOWN_MARKET_BONUS = 0.03;

// web/src/lib/offers.ts — 화면·서버가 공유하는 단일 진입점
export function rateFor(changePct: number, tiers: DiscountTier[]) {
  const tier = depthFor(Math.abs(changePct), tiers);
  const wanted = tier.rate + (moodFor(changePct).side === 'loss' ? DOWN_MARKET_BONUS : 0);
  const rate = Math.min(MAX_DISCOUNT_RATE, wanted);
  return { tier, base: tier.rate, bonus: rate - tier.rate, rate };
}
```

| 국장 마감 | 구간 기본 | 하락장 | 최종 |
|---|---|---|---|
| ▲ +2.20% | 20% | — | **20%** |
| ▼ −0.05% (보합) | 5% | — | **5%** |
| ▼ −0.50% | 10% | +3%p | **13%** |
| ▼ −2.20% | 20% | +3%p | **23%** |
| ▼ −3.10% | 30% | +3%p | **33%** |

보합(±0.1% 미만)에는 붙이지 않는다 — 화면에 '본전가'가 뜨는 날 가격만 내려가면
방향 규칙이 어긋난다. 상한 38%는 `Math.min`으로 한 번 더 막고, 상한에 걸려 깎이면
표시되는 보정 값도 실제로 얹힌 만큼으로 줄어든다.

**기대값 변화 (같은 1,219 거래일 직접 재집계)**

| 구분 | 값 |
|---|---:|
| 하락 마감 (−0.1% 이하) | 506일 · **41.5%** |
| 상승 마감 (+0.1% 이상) | 614일 · 50.4% |
| 보합 (±0.1% 미만) | 99일 · 8.1% |
| **기본 기대값** | **13.5%** |
| **하락 보정 포함 기대값** | **14.7%** |
| 30% 이상 도달 | 113일 → 연 23일 |
| **상한 38% 도달** | **0일** — 상한은 안전장치이고 실측 최대는 33% |

구간별 최종 할인율:

| KOSPI 절대 변동폭 | 상승·보합 | 하락 +3%p | 일수 | 비율 |
|---|---:|---:|---:|---:|
| 0.3% 미만 | 5% | **8%** | 271 | 22.2% |
| 0.3 ~ 0.8% | 10% | **13%** | 342 | 28.1% |
| 0.8 ~ 1.5% | 15% | **18%** | 322 | 26.4% |
| 1.5 ~ 2.5% | 20% | **23%** | 171 | 14.0% |
| 2.5% 이상 | 30% | **33%** | 113 | 9.3% |

---

## 6. 근거 표기

| 항목 | 근거 수준 |
|---|---|
| 구간 경계 0.3 / 0.8 / 1.5 / 2.5% | **실측** — Yahoo `^KS11` 5년 1,218 거래일 직접 집계 |
| 하락장 +3%p | **기획 배분값** — 실측 아님. 38% 상한 안에서 나눈 몫 |
| 각 구간 빈도·연 환산 일수 | **실측** — 위와 동일 |
| 할인율 5 / 10 / 15 / 20 / 30% | 38% 제약 안의 배분값 — 최적값 검증 아님 |
| 최대 할인 38% | **기업 확인값** (2026-09) |

> 발표에서 구간 경계는 *"실측 분포로 잡았습니다"*, 할인율 자체는
> *"38% 한도 안에서 역할별로 배분한 MVP 값입니다"* 로 나눠 말해야 한다.
