# 인수인계 — 2026-09-23

새 세션이 바로 이어받을 수 있게 남긴다. **동작·근거는 `팀공유_빵장_설계도.md`에 있고,
여기는 9/19 이후에 바뀐 것과 거기 안 들어간 맥락만** 적는다.

> 이전 인수인계: `인수인계_2026-09-19.md`
> 발표: **9/30**. 남은 기간 일주일.

---

## 0. 한 줄 요약

9/19 이후 커밋 **44건**. 큰 줄기는 넷이다.

1. **"싸다고 보여주고 정가로 보낸다"를 끝냈다** — 자사몰 판매가를 실제로 바꾸고, 옵션 추가금까지 같은 비율로 깎는다
2. **자사몰이 파는 단위를 그대로 판다** — 빵장에만 있던 "− 11 +"를 없애고, 카페24 옵션(1개/3개/5개, 맛, 구성)을 읽어 보여준다
3. **선착순을 진짜로 만들었다** — 1시간 결제 기한 · 옵션별 자리 · 한 사람 한 자리. 동시 예약 시험으로 검증한다
4. **웹 푸시 알림**을 붙였다 (덱 3·6·8·12장의 "가격 알림"이 없던 기능이었다)

커밋 메시지에 "왜 그렇게 했는지"를 적어 뒀다 — `git log`가 설계 기록이다.

---

## 1. 지금 켜져 있는 것 / 꺼져 있는 것

### 코드 스위치 (`.ts` 상수 — 환경변수로 못 켠다)

| 위치 | 값 | 왜 |
|---|---|---|
| `coupon.ts` `COUPON_ENABLED` | **false** | 할인코드는 회원 전용이라 비회원이 못 쓴다. 결제창에 코드를 옮겨 적는 단계를 빵 한 개 사는 흐름에 넣지 않는다. **코드는 지우지 않았다** — 판매가 변경 승인이 끝내 안 나면 이게 유일한 수단이다 |
| `priceSync.ts` `PRICE_SYNC_ENABLED` | false | 환경변수 `PRICE_SYNC=on`으로 켠다. 상수를 true로 박으면 실수로 실제 몰 가격이 바뀐다 |
| `offers.ts` `LINE_MODE` | `'line'` | 상승=식사형 / 하락=달달. `'all'`이면 라인 구분 없이 전 상품 |

### 환경변수 (로컬 `.env.local` 기준)

| 키 | 값 | 비고 |
|---|---|---|
| `NEXT_PUBLIC_SHOP_BASE` | `https://makjitest.cafe24.com` | **실제 막지몰 계정을 받으면 이것만 바꾸면 된다.** 안 넣으면 코드 기본값 `makji.kr`로 간다 — 우리가 못 만지는 몰이다 |
| `PRICE_SYNC` | `on` | 15:30에 자사몰 판매가·옵션 추가금을 내린다. 00:10에 복원 |
| `DISCOUNT_DELIVERY` | `price` | 판매가 변경으로 전달. `coupon`으로 바꿔도 `COUPON_ENABLED=false`라 안 나간다 |
| `INVENTORY_SYNC` | **`off`** | 아래 §3 참고. **켜지 마라** |
| `CAFE24_ORDER_SCOPE` | `on` | 주문 조회 권한(`mall.read_order`). 개발자센터에 등록돼 있고 토큰에도 들어갔다 |
| `MARKET_ALWAYS_OPEN` | `on` | **로컬 시험용.** 배포본에 넣지 말 것 — 장이 24시간 열린다 |
| `NEXT_PUBLIC_VAPID_PUBLIC_KEY` / `VAPID_PRIVATE_KEY` / `VAPID_SUBJECT` | 있음 | 웹 푸시. 비밀키는 Vercel에서 Secret |

### 관리자 화면에서 정하는 값 (`app_settings` 표)

| 키 | 기본값 | 화면 |
|---|---|---|
| `daily_allotment` | 30 | `/admin` 가격 목록의 `예약 n건`. 열쇠가 `"32"`(빵 전체) 또는 `"32:품목코드"`(그 옵션만)다 |
| `dividend_aov` / `dividend_rate` / `dividend_budget` | 8,000원 / 10% / 50만원 | 주말 배당. 8,000 × 10% = **800P**로 발표덱 8장과 맞춰 뒀다 |
| `ipo_enabled` | **false** | 공모주 보류. 끄면 `/admin`의 회차 관리도 같이 사라진다 |

---

## 2. 9/19 이후 고친 것 — 유형별

같은 종류의 버그가 계속 나왔다. **"화면이 말하는 것과 실제가 다르다"** 하나다.

### ① 화면이 거짓말하던 것

| 무엇 | 실제로는 | 커밋 |
|---|---|---|
| "막지몰로 구매하러 가기" | 우리가 못 만지는 `makji.kr`로 보냈다 | `464d598` |
| "오늘 5% 할인" | 5개 옵션을 고르면 **1.2%**였다 (추가금을 안 깎아서) | `7ef0395` |
| "46,970원에 구매하기" | 자사몰은 **40,730원**이었다 (묶음 할인을 무시하고 단가×개수) | `acb1aa0` |
| "예약마다 할인코드가 발급됩니다" | 쿠폰은 껐다 | `613c08b` |
| 관리자 가격 | 손님과 **다른 계산**이었다 (`buildDailyPlan` vs `buildToday`) | `4ad6879` |

### ② 자기 발등을 찍던 것

| 무엇 | 왜 | 커밋 |
|---|---|---|
| 예약이 자사몰 재고를 깎았다 | 손님이 실제로 사면 카페24가 **또** 깎아 재고가 2 줄었다. 예약만 쌓이면 0이 되어 **예약자 본인이 품절을 만난다** | `1b8040b` |
| `adjustInventory`가 **항상 첫 품목**을 깎았다 | 주석엔 "막지 상품은 옵션이 하나뿐"이라 적혀 있었지만 대만식 샌드위치는 여덟이다 | `acb1aa0` |
| 출석 점수가 아무에게도 안 붙었다 | 방문자 표식을 예약·관심 담기에서만 발급해, 둘러보는 사람에겐 영영 안 생겼다 | `7b9c3f9` |
| 만료 예약이 배당 구매 점수로 셌다 | 안 산 사람에게 배당이 나갔다 | `10c4a79` |

### ③ 선착순을 진짜로 만든 것

- `db61566` 1시간 결제 기한 (`fills.expires_at` · `settled`)
- `65156c3` 자리를 **옵션별**로 센다 + **한 사람 한 자리**
- `19b41b9` 경합에 밀린 사람이 다음 칸으로 올라간다 — 안 그러면 **50명이 30자리에 달려들 때 12명만** 들어갔다
- `4ad6879` 예약 복원 — 같은 빵을 다시 누르면 오류 대신 그 예약을 열어준다

### ④ 새 기능

- `3429370` **웹 푸시 알림** — 관심빵이 할인되면 15:30에 보낸다
- `958af9e` 관리자가 목록에 빵을 넣고 뺀다 (오늘 라인은 자동이지만 기업이 안 따른다)
- `b0c0434` 물량을 빵마다 / 옵션마다

---

## 3. ⚠️ 켜지 말 것 · 손대지 말 것

### `INVENTORY_SYNC` — 주문 연동 전까지 켜지 마라

예약할 때 자사몰 재고를 깎는 기능이다. 지금 켜면 이렇게 된다.

```
손님이 예약    재고 30 → 29   우리가 깎음
손님이 결제    재고 29 → 28   카페24가 또 깎음
→ 빵 하나 팔고 재고 2 줄어듦. 30개 풀면 15개에서 품절
```

더 나쁜 쪽 — 예약만 쌓이고 결제가 안 되면 재고가 0이 되는데, **재고 0이면 손님 화면에 품절이 뜬다**(9/22 체험몰 실측). 예약한 사람이 사러 갔더니 품절이다.

되살리는 조건: **주문 조회로 "이 예약이 결제됐다"를 확인하고 가차감을 되돌리는 정산**이 붙은 뒤. 코드는 그대로 두었으니 스위치만 올리면 된다.

### `fills`의 유니크 인덱스 — 선착순 경합을 막는 핵심

```sql
fills_slot_unique       (day, product_no, depth, coalesce(unit,''), slot)
fills_one_per_visitor   (day, product_no, visitor) where visitor is not null
                                                     and settled is distinct from 'expired'
```

- `coalesce(unit,'')`인 이유 — 그냥 `unit`을 넣으면 포스트그레스가 NULL을 서로 다른 값으로 봐서, **옵션이 없는 상품에서만 경합 잠금이 조용히 풀린다**
- `settled <> 'expired'`인 이유 — 그 줄까지 막으면 결제를 못 한 사람이 **그날 다시는 예약을 못 한다**

여기를 고치면 **반드시 `scripts/race-test.mjs`를 돌릴 것.**

### `settle.ts`의 만료 판정 — 지금은 근거 없는 추정이다

```
쿠폰이 없다        → 반납.  다시 물어봐도 영영 답이 없다(판매가 모드엔 쿠폰이 없다)
쿠폰은 있는데 못 읽음 → 붙들기. 카페24가 잠깐 안 될 수 있다
```

쿠폰 없는 예약을 반납하는 것은 **판매가로 이미 결제한 손님의 자리를 뺏을 수 있다.** 그래도 이쪽을 고른 이유는 손해의 크기가 달라서다 — 잘못 반납해도 손님은 이미 샀고(주문은 자사몰에 남는다) 되돌릴 쿠폰도 없지만, 안 풀면 **선착순이 통째로 멈춘다**.

한때 "모르면 붙들기"로 바꿔 놨다가 **1시간 기한이 통째로 죽은 적이 있다**(모든 예약에 쿠폰이 없으므로). 주문 조회가 붙으면 이 추정은 사라진다.

---

## 4. 시험 도구

| 스크립트 | 무엇 | 쓰는 법 |
|---|---|---|
| `scripts/race-test.mjs` | **동시 예약** — 초과·과소 체결, 자리 번호 중복, 멱등성 | `node --env-file=.env.local scripts/race-test.mjs 50`<br>`… --clean` 으로 남은 예약 정리 |
| `scripts/verify-reservations.ts` | 예약 복원·만료 판정 단위 시험 (네트워크 없음) | `npx tsx scripts/verify-reservations.ts` |
| `scripts/ux-check.py` | 화면 스모크 (Playwright, 390px) | `python3 scripts/ux-check.py` — **아직 안 돌려봤다** |
| `scripts/cafe24.mjs` | 체험몰 직접 조회 | `node scripts/cafe24.mjs` / `… codes` |
| `scripts/verify-cafe24-products.mjs` | 상품·옵션 대조 | |
| `scripts/verify-watches.ts` | 관심 담기 집계 | |

**날짜 함정** — `fills.day`는 **KST**다(`seoulDateString`). 정리 쿼리를 UTC로 짜면 자정 전후에 엉뚱한 날을 건드리고, 지운 줄 알았는데 그대로다. 실제로 그 함정에 빠져 앱이 고장 난 줄 알고 한참 헤맸다.

---

## 5. 카페24 — 되는 것과 안 되는 것

### 되는 것 (Admin API)

상품 만들기·고치기·지우기 / 판매가 / **옵션 추가금**(`variants[].additional_amount`) / 재고 / 할인코드 / **주문 조회**

### 안 되는 것

| 무엇 | 왜 |
|---|---|
| 분류(카테고리) 만들기 | `403 insufficient_scope`. 개발자센터에서 권한을 안 받았다 — 한 번 손으로 만들면 되는 일이라 안 받기로 했다 |
| **손님 장바구니에 담기** | 장바구니는 손님 **브라우저 세션**에 붙어 있다. 로그인이 없는 우리 서비스는 그 세션을 모른다. `/exec/front/order/basket/`에 쏴봤지만 `{"result":0,"isLogin":"F"}`로 거절(9/23 확인) |
| **상품 링크에 옵션 미리 선택** | `option1` · `opt_id` · `option_id` · `selected_option1` 네 가지 모두 안 먹는다(9/23 확인). **손님은 막지몰에서 옵션을 한 번 더 골라야 한다** |
| 디자인·테마·몰 설정 | API 영역이 아니다 |

건진 것 하나 — 자사몰 옵션 `<select>`의 값이 **우리가 `fills.unit`에 저장하는 품목코드와 똑같다**(`P00000BG000G`). 나중에 손님 브라우저에서 직접 쏘는 방식을 쓸 거면 값은 이미 다 갖고 있다.

### 인증

- 재인증: `https://korea-traditional.vercel.app/api/cafe24/authorize`
- **Redirect URI는 배포 주소만 등록돼 있다.** localhost는 `https`만 받아서 등록이 안 된다 — 로컬에서 인증 못 한다
- **refresh token은 2주짜리다.** 만료 전에 재인증할 것. 누르기 전에 `/api/health`의 `scopes`에 `mall.read_order`가 있는지 확인 — 없는 채로 동의하면 쓸모없는 토큰을 받고 **토큰 요청 횟수(2시간 15회)만 깎인다**

---

## 6. 발표덱과 코드 — 맞춰 둔 것

`deck/MAKJI_최종발표덱_V9_완성본.pptx` (원본은 `.백업.pptx`)

| 덱 | 고친 내용 |
|---|---|
| 8장 점수표 | 5항목 → **실제로 되는 3항목**. 장바구니·알림은 `2단계`로 표기. 구매 가중 +3 → +1 |
| 8장 등급 | 점수 구간을 만점 3점에 맞춤 (0/1/2/3점). 금액 300·500·800P는 덱 그대로 |
| 15장 | 대만식 샌드위치 2,400 → **2,600원** |

**구매에 3배 가중을 안 준 이유**(발표에서 쓸 만한 논거) — 빵은 한 주에 두 번 사는 물건이 아니다. 구매자에게 최대 배당을 주면 **이번 주에 다시 살 가능성이 가장 낮은 사람에게 가장 많이 주게 된다.** (`lib/dividend.ts` 주석)

**장바구니를 점수 항목에서 뺀 이유** — 카페24 장바구니는 카페24 회원 것이고, 우리 손님은 익명 쿠키뿐이라 **연결할 방법이 없다**.

---

## 7. 다음에 할 일

### 🔴 발표 전에 하면 좋은 것

1. **주문 연동 정산** — 주문 조회로 결제를 확인해 `settled='paid'` 처리하고, 가차감을 되돌린다. 이게 붙으면
   - `settle.ts`의 추정이 사라진다
   - `INVENTORY_SYNC`를 다시 켤 수 있다
   - `skuSignals`의 판매속도가 예약(대리값)이 아니라 실제 판매가 된다
   **체험몰에 주문이 0건이라 데이터 모양을 못 봤다.** 관리자 → 주문 → 주문등록으로 한 건만 넣으면 시작할 수 있다

2. **웹 푸시 실제 발송 확인** — 관심빵을 담고 🔔를 켠 뒤 15:30 크론을 당겨 폰에 뜨는지 본다
   ```
   curl -H "Authorization: Bearer $CRON_SECRET" <배포주소>/api/cron/publish
   ```

3. **`ux-check.py`를 한 번 돌려볼 것** — 아직 안 돌렸다

### 🟡 여유 있으면

- **옵션별 물량 입력이 실제로 쓰이는지** 확인 — 만들어는 뒀지만 아직 아무도 값을 안 넣었다
- `MARKET_ALWAYS_OPEN`이 배포본에 없는지 확인
- 카페24 옵션 이름 오타: `믹지 글루텐프리 스콘 5개 set` (막지 → 믹지). 카페24 관리자에서 고치면 빵장도 따라온다

### 🟢 기업에서 받아야 하는 것

- **실제 막지몰 카페24 계정** — 받으면 `NEXT_PUBLIC_SHOP_BASE`만 바꾸면 된다
- **한 달 판매 집계 원본** — 배당 객단가(지금 8,000원)가 관측값이 아니라 제안값이다 (덱 17장)
- 상품별 원가·마진 상한 — 할인 상한 38%가 마진을 깨는지 확인할 근거가 없다

---

## 8. 여러 에이전트가 같은 저장소를 편집한다

9/19 인수인계에도 적혀 있던 문제가 계속됐다. 9/22~23에도 다른 에이전트(코덱스)가 14개 파일을 고쳤다.

**충돌은 없었고 대체로 개선이었다** — 예약 복원, 관리자 계산 통일 등. 다만 **만료 판정을 바꾸면서 1시간 기한이 통째로 죽은 것**을 뒤늦게 발견했다(§3).

**새 세션이 할 일** — 시작하자마자
```
git status          # 커밋 안 된 남의 작업이 있는지
git log --oneline -5
node --env-file=web/.env.local web/scripts/race-test.mjs 50   # 예약 경합이 살아 있는지
npx tsx web/scripts/verify-reservations.ts
```

시험이 통과하는지부터 보고 시작하는 것이 제일 빠르다. 오늘 실제로 그렇게 버그 두 개를 잡았다.
